# Snapshot di sincronizzazione

> Da leggere per primo a inizio sessione. Fotografa lo stato del progetto al commit di riferimento e mappa ogni scheda al suo stato di verifica. È la fonte di verità su cosa è fatto, non le spunte del diario.

## Stato

```
Branch attivo:         main
Commit di riferimento: 9acc2e0 (schede riancorate con sync-context il 2026-08-05)
HEAD al 2026-08-06:    3bb62ac (tre commit oltre l'ancoraggio, vedi la nota di verifica)
Data snapshot:         2026-08-06
```

## Stato di verifica delle schede

| Scheda | last-verified | Stato |
|---|---|---|
| STACK.md | 9acc2e0 | gap chiuso il 2026-08-06: `tools/**` e ora fra le `covers-paths` e la scheda descrive gli undici script in una sezione propria. Il contenuto e stato scritto leggendo i file, non riconciliato da un diff, quindi il confronto di drift su `tools/` parte dal prossimo commit |
| design-and-security.md | 9acc2e0 | nessun drift; diagrammi registrati e riconciliati, paradigmi da popolare |
| deployment.md | 017b02a | da popolare (covers-paths vuoto, nessun confronto di drift applicabile) |
| dev-testing.md | 017b02a | da popolare (covers-paths vuoto, nessun confronto di drift applicabile) |
| current-work.md | 9acc2e0 | aggiornata il 2026-08-05 con la voce sulla rigenerazione di `armonia-libro` |
| roadmap.md | 017b02a | aggiornata (covers-paths vuoto, nessun confronto di drift applicabile) |

**Sync del 2026-08-05**: confronto `4942de1..9acc2e0` per STACK.md e design-and-security.md, `dd1c4d5..9acc2e0` per current-work.md. Zero file cambiati nelle aree coperte da ciascuna scheda, quindi il bump e un checkpoint e non una riconciliazione. Le tre schede con `covers-paths` vuoto restano fuori dal confronto e non si bumpano.

**Verifica del 2026-08-06**: l'utente ha chiesto se la descrizione corrente del progetto fosse ancora esatta, e ogni numero e ogni affermazione operativa e stata ricontrollata sui file reali. Tengono tutti i conteggi (172 voci nel registro con 99, 65 e 8; 101 voci nel `.bib` e nessun orfano nelle due direzioni; 19 voci di cassaforte; 18 ragionamenti; 14 skill di cui 10 con `disable-model-invocation: true`; 9 ADR; 8 script sotto `tools/`; 6 figure in `_ly-figure/`; 6 coppie di backup del capitolo continuo; 10 PDF in `fonti-esterne/`). Tre affermazioni sono state corrette, cioè il significato del conteggio dei caratteri del capitolo, il livello di verifica nel campo `note` del `.bib` e il numero dei ragionamenti in attesa, tutte trattate nelle righe sotto. Sul versionamento: fra l'ancoraggio `9acc2e0` e HEAD `9f5420b` stanno `6f9c2bc`, che ha propagato l'aggiornamento dei template, e `9f5420b`, che ha riformattato l'intero albero tracciato mettendo ogni paragrafo su una riga sorgente continua. Il secondo ha toccato `.claude/skills/**`, che è nelle `covers-paths` di `current-work.md`, quindi un passaggio di `sync-context` e dovuto: il cambiamento e pero puramente tipografico e non c'è drift di contenuto da riconciliare. Con il commit `3bb62ac` del 2026-08-06 le aree coperte cambiate diventano due, perché `tools/**` e ora nelle `covers-paths` di `STACK.md` e vi sono entrati tre script: il primo `sync-context` della sessione nuova bumpa entrambe le schede a `3bb62ac`, sapendo che per `tools/` non è una riconciliazione ma il punto di partenza del confronto. La convenzione introdotta da `9f5420b` viveva solo nel messaggio di commit: e stata scritta lo stesso giorno in `.claude/rules/interaction-style.md`, con le due ragioni e le eccezioni, perché altrimenti non sarebbe sopravvissuta alla sessione.

**Tre interventi del 2026-08-06, autorizzati dall'utente dopo la verifica.** Il primo chiude il gap più vecchio dello snapshot, segnalato dal 2026-07-17: `STACK.md` ha ora `tools/**` fra le `covers-paths` e una sezione che descrive ciascuno strumento, distinguendo i due che implementano affermazioni del libro per ADR-009, i due che tengono in pari gli artefatti derivati, i tre dell'ingestione documentale e i due di servizio, e dichiarando che nessuno di essi entra nella catena di build. Il secondo scrive la convenzione Markdown in `interaction-style.md`. Il terzo crea `tools/latest-screenshot.ps1`, che `.claude/rules/manual-screenshots.md` presupponeva da sempre senza che il file esistesse: restituisce percorso, data, eta e peso dell'ultima immagine della cartella di cattura, accetta `-Folder` per le macchine che salvano altrove, `-MaxAgeMinutes` per distinguere lo screenshot appena richiesto da uno vecchio con exit code 2, e `-PathOnly`; provato nei quattro percorsi, compreso quello di cartella inesistente. Nella stessa giornata l'utente ha aggiunto `tools/md-unwrap.py`, che applica la convenzione srotolando i paragrafi con un oracolo di rendering a garanzia, e `tools/lint-md-commands.py`, che copre l'angolo che il primo per contratto non tocca, cioè i comandi spezzati dentro i blocchi recintati, insieme all'estensione della regola `git-commands-format.md` che vieta di spezzare un comando su più righe. Con questi tre strumenti gli script sotto `tools/` passano da otto a undici, e il `README.md` della cartella rimanda a `STACK.md` per il quadro completo.

**Drift chiuso il 2026-07-17**: i quattro commit intercorsi (`f3a6c45`, `f2d8d9c`, `bb78fca`, `2c49269`, `4942de1`) sono stati riconciliati con `sync-context`. L'unico cambiamento sostanziale nelle aree coperte era l'ambiente `apertura` aggiunto a `style/harmony-macros.sty` (introdotto in `f3a6c45`), ora documentato in `STACK.md`; i diagrammi in `design-and-security.md` erano già corretti nel contenuto, solo l'ancoraggio era stale. `deployment.md`, `dev-testing.md` e `roadmap.md` non hanno `covers-paths` popolati e restano fuori da questo confronto.

## Punto di ripresa

Aggiornato a mano il 2026-08-06. Il ciclo di lavoro fitto va dal 2026-07-29 al 2026-08-03; il 2026-08-05 e il 2026-08-06 non hanno aggiunto contenuto al libro, hanno rimesso in pari la skill `armonia-libro`, sanato quattro note stale e verificato i fatti del progetto contro i file, come raccontano le due voci in testa al work-log. La guida operativa alla ripresa e la nota del 2026-08-06 in testa a `_notes/RESUME-PROMPT.md`, e la guida alla rilettura del capitolo e `_notes/STATO-CAPITOLO-TRITONO.md`, rivista il 2026-08-05.

Una cosa da portare al template e non a questo repository, aperta il 2026-08-06: `python tools/lint-md-commands.py .claude` segnala che in `.claude/PROJECT-SYSTEM.md`, sezione 15, il comando di wipe totale sta su tre righe unite da backslash, e copiandone solo la prima si ottiene un wipe parziale che sembra completo. Va unito in una riga sola in `template-claude-developing` e propagato, perché qui il file e una copia arrivata con `6f9c2bc`. La segnalazione su `skills/init-project-system/SKILL.md` riga 62 e invece un falso positivo dell'euristica.

Una cosa da sapere prima di usare gli strumenti di freschezza, perché e costata un ciclo di deriva invisibile: `python tools/skill-freshness.py <skill> --update` riallinea gli hash e non tocca il contenuto, quindi si lancia soltanto dopo aver rigenerato i file della skill, mai prima. Il 2026-08-03 e stato lanciato senza rigenerare, e l'intero lavoro di quel giorno e rimasto fuori da `armonia-libro` fino al 2026-08-05.

Ambiente e catena di build LaTeX/LilyPond verificati (ADR-003). Il capitolo sul tritono e una bozza matura, nove movimenti e 48314 caratteri di prosa, dove il conteggio misura i movimenti e non il file, che intero e di 51156 byte; ma vive ancora in `_notes/appunti-da-inserire-nel-libro/` e NON e nel manoscritto: `manuscript/chapters/` contiene solo introduzione e scheletro del capitolo 1, e `build/main.pdf` e fermo al 2026-07-16. La trascrizione in `.lytex` resta il passo più urgente e più rinviato.

Cosa e cambiato in questo ciclo. Sono nate tre skill: `armonia-libro`, che digerisce la dottrina del libro dell'utente, `libro-berkman`, che rende interrogabile Berkman 2013, e `fonte-nuova`, che è la procedura per far entrare una fonte dichiarandone il livello di verifica. Le prime due sono ignorate da git per ADR-007 perché sono contenuto, la terza e tracciata perché e procedura. La bibliografia e passata da 91 a 101 voci, con sette fonti nuove. Sul livello di verifica va detto con precisione, perché la formulazione precedente di questa riga era troppo generosa e il 2026-08-06 e stata corretta sui file: il livello e dichiarato per tutte e 101 le voci nel registro, campo `bib_verified_by`, dove 89 portano `colophon` e dodici una lettura diretta o equivalente, mentre nel campo `note` del `.bib` la formula in maiuscolo compare solo nelle 15 voci toccate dal 2026-07-31 in avanti, cioè da quando ADR-008 esiste; nelle altre 86 il `note` porta provenienza ed edizione, non il livello. Sono nati quattro strumenti Python che implementano affermazioni del libro invece di documentarle, secondo ADR-009, e due di essi hanno cambiato il contenuto del capitolo: `tritoni-scale.py` ha smentito l'intuizione sulla corrispondenza biunivoca fra scale e coppie di tritoni, `derivazione-scale.py` ha fatto emergere la scala napoletana minore, che era registrata come direzione futura sospesa per mancanza di fonti.

Il risultato di contenuto più importante: girando la macchina delle dominanti secondarie su tutti i gradi di Do maggiore, con una sola alterazione per accordo, escono due tonalita confinanti e quattro scale derivate, cioè due minori melodiche, una minore armonica e la napoletana minore. La spiegazione e che il tritono di una scala diatonica e formato dai due estremi della catena di quinte: alterare un estremo fa slittare la catena e produce la tonalita vicina, alterare un interno la spezza e produce una scala nuova con due tritoni.

Tre correzioni dell'agente in questo ciclo, tutte registrate in `_notes/STATO-CAPITOLO-TRITONO.md`, l'ultima rilevata dall'utente e formalizzata in ADR-009. Da tenere presente alla ripresa: quando si usa la macchina delle derivazioni, la definizione di dominante e quella del libro, cioè il tritono fra terza maggiore e settima minore, non la quinta giusta.

Cosa aspetta l'utente: materiale privato sulla scala napoletana, annunciato e non consegnato, che sblocchera l'espansione della tabella con le armonizzazioni. Cosa aspetta una sua parola: l'analisi del livello II della macchina, calcolata e rinviata; il risultato 4 sui tre tipi di coppia di tritoni, calcolato e mai proposto perché toccherebbe i movimenti F e H insieme; sei ragionamenti che aspettano una decisione in `_notes/ragionamenti-da-portare-nel-libro.md`, e cioè i cinque in stato "da proporre" (R1, R3, R4, R12, R13) più R6 che è in stato "proposto" con il testo esatto pronto nella voce 12 della cassaforte; se spezzare il movimento E, oggi al 22 per cento; e due nodi bibliografici da sciogliere prima di andare in LaTeX, cioè il riferimento [8] che copre due fonti Sarti distinte e la fonte Springsteen che nel capitolo e [10] ma non ha voce nel registro.

Nota sui numeri bibliografici, ricalcolati sui file reali il 2026-08-05 e da preferire a quelli scritti altrove: il registro ha 172 voci, di cui 99 verificate, 65 da verificare e 8 scartate, e `manuscript/bib/references.bib` ne contiene 101.

A ogni avanzamento significativo sulla stesura vera e propria: aggiornare le schede impattate e il work-log, poi l'utente committa e si rilancia `sync-context` per bumpare `last-verified-commit`. Da collaudare ancora la parità Linux degli script `.sh`. Da riconciliare `STACK.md`, che non descrive i quattro strumenti nuovi sotto `tools/`.
